## 你问“我能否阅读项目里的 Excel？”
- 可以：我能把项目中的 .xlsx 文件逐个 Sheet 读取出来（包括表头、字段、枚举、说明、关系），并将其结构化融入 PRD。
- 但当前处于计划模式：我只能做只读勘察，不能运行解析 Excel/PDF/大图的操作；需要你确认后我才能执行解析与重写。

## 已发现的 reference 资料（只读勘察结果）
- `reference/DATA_INGEST_ARCHITECTURE.md`：明确了“分离式 SIP（file + manifest JSON）”“双重校验（客户端哈希 vs 服务端哈希）”“解包存储”“按需动态生成 BagIt ZIP”“IIIF 友好”等入库架构。
- `reference/影像系统描述和管理元数据架构.xlsx`：需读取全部 Sheet 获取元数据字段、分类、映射关系。
- `reference/资源管理需求表.xlsx`：需读取全部 Sheet 获取模块需求清单与优先级。
- `reference/文物二维影像元数据思维导图.png`、`reference/架构流程图-流程图.jpg`：需做 OCR/结构提取（其中 jpg 文件较大，需专门处理）。
- `reference/MDAMS系统概要设计文档（Museum+Digital+Asset+Management+System）.pdf`：需提取目录/关键章节要点并对齐 PRD。

## 重写 PRD 的目标（按你要求：参考 reference 全量资料 + Excel 全部 Sheet）
- 以“平台 → 系统 → 模块”作为主结构。
- 对齐并显式落地：
  - 入库与传输架构（分离式 SIP、Fixity、按需 BagIt、IIIF 友好）。
  - 影像元数据体系（管理/技术/保存/描述/结构元数据）。
  - 需求表中的功能清单、优先级、范围边界与验收。
  - 概要设计文档中的系统边界、数据流、集成点。

## 我将如何执行（你确认后）
1. **解析 Excel（两份 .xlsx，含全部 Sheet）**
   - 导出：Sheet 列表、每个 Sheet 的表结构、字段定义、枚举、约束、关系。
   - 产出：
     - PRD 的“数据字典/元数据字典”章节（按字段分组、必填、校验、示例）。
     - PRD 的“需求清单/优先级/验收”章节（按模块映射）。
2. **提取图片与流程图信息（PNG/JPG）**
   - 对思维导图/流程图做 OCR + 结构化抽取：平台/系统/模块、关键流程节点、输入输出。
   - 将其落到 PRD 的“总体架构图（Mermaid）+ 流程（Mermaid）+ 状态机/异常路径”。
3. **提取 PDF 要点并对齐**
   - 抽取目录、关键章节要点（系统范围、模块说明、接口/部署/非功能等）。
   - 与 Excel/图片结果进行冲突消解：以 reference 为准，必要时在 PRD 中标“待确认”。
4. **按参考资料重写 PRD（全量重写，不是增补）**
   - 结构：一页摘要 → 目标/指标 → 平台/系统/模块 → 关键流程（含入库）→ 数据/元数据字典 → 权限/审核 → NFR → 验收 → 风险。
   - 重点把 reference 中“入库架构”写进流程与验收（客户端/服务端双重哈希、解包存储、按需 BagIt 导出、IIIF 预览）。
5. **落地文件与回溯链**
   - 新建或覆盖 PRD 文档（建议生成新文件以保留历史版本）。
   - 在 PRD 中标注“来自哪个 reference（文件/Sheet/图）”的来源映射表（不写代码注释）。

## 交付物（你确认后会得到）
- 一份重写后的 PRD（Markdown），完整覆盖 reference/ 下文件与 Excel 全部 Sheet。
- 一份“reference → PRD 章节映射表”（便于评审追溯）。

如果你确认这个计划，我将开始执行解析 Excel/PDF/图片并重写 PRD。